package errors

import "errors"

// Перечень ошибок, используемых во всех пакетах MCP Chain

var (
	// Ошибка: неверная транзакция
	ErrInvalidTx = errors.New("неверная транзакция")

	// Ошибка: недостаточно стейка для валидатора
	ErrInsufficientStake = errors.New("недостаточно стейка для валидатора")

	// Ошибка: недостаточно баланса для транзакции
	ErrInsufficientBalance = errors.New("недостаточно баланса для транзакции")

	// Ошибка: неверная подпись
	ErrInvalidSignature = errors.New("неверная подпись")

	// Ошибка: транзакция уже существует
	ErrTxExists = errors.New("транзакция уже существует")

	// Ошибка: неизвестная команда или метод
	ErrUnknownCommand = errors.New("неизвестная команда или метод")

	// Можно добавлять другие ошибки по мере необходимости
)
